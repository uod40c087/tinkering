# Git Workflow Tips

- Use `git pull --rebase` to keep history linear.
- Commit small, focused changes with clear messages.
- Always run `git status` before committing to catch stray files.
- Leverage `git stash` to switch contexts without losing work.
- Review diffs with `git diff --stat` for a quick sanity check.